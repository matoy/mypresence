{
  "__inputs": [
    {
      "name": "DS_PROMETHEUS",
      "label": "Prometheus",
      "description": "",
      "type": "datasource",
      "pluginId": "prometheus",
      "pluginName": "Prometheus"
    }
  ],
  "__requires": [
    { "type": "grafana", "id": "grafana", "name": "Grafana", "version": "9.0.0" },
    { "type": "datasource", "id": "prometheus", "name": "Prometheus", "version": "1.0.0" },
    { "type": "panel", "id": "timeseries", "name": "Time series", "version": "" },
    { "type": "panel", "id": "stat", "name": "Stat", "version": "" },
    { "type": "panel", "id": "gauge", "name": "Gauge", "version": "" },
    { "type": "panel", "id": "table", "name": "Table", "version": "" },
    { "type": "panel", "id": "piechart", "name": "Pie chart", "version": "" },
    { "type": "panel", "id": "barchart", "name": "Bar chart", "version": "" }
  ],
  "annotations": { "list": [] },
  "description": "myPresence – Operational dashboard (HTTP, Auth, Business, DB)",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [

    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 }, "id": 1, "title": "🔢 Overview", "type": "row" },

    {
      "id": 2, "type": "stat",
      "title": "Request rate (1m)",
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 1 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "orientation": "auto", "colorMode": "background", "graphMode": "area", "textMode": "auto" },
      "fieldConfig": { "defaults": { "unit": "reqps", "color": { "mode": "thresholds" }, "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "yellow", "value": 10 }, { "color": "red", "value": 50 }] } } },
      "targets": [{ "expr": "sum(rate(mypresence_http_requests_total[1m]))", "legendFormat": "RPS", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 3, "type": "stat",
      "title": "HTTP error rate (5m)",
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 1 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background", "graphMode": "area" },
      "fieldConfig": { "defaults": { "unit": "percentunit", "color": { "mode": "thresholds" }, "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "yellow", "value": 0.01 }, { "color": "red", "value": 0.05 }] } } },
      "targets": [{ "expr": "sum(rate(mypresence_http_requests_total{status_class=~\"4xx|5xx\"}[5m])) / sum(rate(mypresence_http_requests_total[5m]))", "legendFormat": "Error rate", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 4, "type": "stat",
      "title": "P95 latency (5m)",
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 1 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background", "graphMode": "area" },
      "fieldConfig": { "defaults": { "unit": "s", "color": { "mode": "thresholds" }, "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "yellow", "value": 0.5 }, { "color": "red", "value": 2 }] } } },
      "targets": [{ "expr": "histogram_quantile(0.95, sum by(le) (rate(mypresence_http_request_duration_seconds_bucket[5m])))", "legendFormat": "P95", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 5, "type": "stat",
      "title": "Active sessions",
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 1 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "blue" } } },
      "targets": [{ "expr": "mypresence_db_sessions_active_total", "legendFormat": "Sessions", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 6, "type": "stat",
      "title": "Total users",
      "gridPos": { "h": 4, "w": 4, "x": 16, "y": 1 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "purple" } } },
      "targets": [{ "expr": "mypresence_db_users_total", "legendFormat": "Users", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 7, "type": "stat",
      "title": "Goroutines",
      "gridPos": { "h": 4, "w": 4, "x": 20, "y": 1 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "thresholds" }, "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "yellow", "value": 50 }, { "color": "red", "value": 200 }] } } },
      "targets": [{ "expr": "go_goroutines", "legendFormat": "Goroutines", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },


    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 5 }, "id": 10, "title": "🌐 HTTP Traffic", "type": "row" },

    {
      "id": 11, "type": "timeseries",
      "title": "Requests / second — by status class",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 6 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["lastNotNull", "mean"] } },
      "fieldConfig": { "defaults": { "unit": "reqps", "custom": { "lineWidth": 2, "fillOpacity": 10 } } },
      "targets": [
        { "expr": "sum by(status_class) (rate(mypresence_http_requests_total[2m]))", "legendFormat": "{{status_class}}", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },

    {
      "id": 12, "type": "timeseries",
      "title": "Latency percentiles (P50 / P95 / P99)",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 6 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["lastNotNull", "mean", "max"] } },
      "fieldConfig": { "defaults": { "unit": "s", "custom": { "lineWidth": 2 } } },
      "targets": [
        { "expr": "histogram_quantile(0.50, sum by(le) (rate(mypresence_http_request_duration_seconds_bucket[5m])))", "legendFormat": "P50", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "histogram_quantile(0.95, sum by(le) (rate(mypresence_http_request_duration_seconds_bucket[5m])))", "legendFormat": "P95", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "histogram_quantile(0.99, sum by(le) (rate(mypresence_http_request_duration_seconds_bucket[5m])))", "legendFormat": "P99", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },

    {
      "id": 13, "type": "table",
      "title": "Top routes — rate, errors, P95",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 14 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "sortBy": [{ "displayName": "RPS", "desc": true }], "footer": { "show": false } },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          { "matcher": { "id": "byName", "options": "RPS" }, "properties": [{ "id": "unit", "value": "reqps" }, { "id": "decimals", "value": 3 }] },
          { "matcher": { "id": "byName", "options": "Error rate" }, "properties": [{ "id": "unit", "value": "percentunit" }, { "id": "decimals", "value": 2 }, { "id": "custom.displayMode", "value": "color-background" }, { "id": "thresholds", "value": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "yellow", "value": 0.01 }, { "color": "red", "value": 0.05 }] } }] },
          { "matcher": { "id": "byName", "options": "P95" }, "properties": [{ "id": "unit", "value": "s" }, { "id": "decimals", "value": 3 }] }
        ]
      },
      "targets": [
        { "expr": "sum by(method, path) (rate(mypresence_http_requests_total[5m]))", "legendFormat": "", "instant": true, "format": "table", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "refId": "A" },
        { "expr": "sum by(method, path) (rate(mypresence_http_requests_total{status_class=~\"4xx|5xx\"}[5m])) / sum by(method, path) (rate(mypresence_http_requests_total[5m]))", "legendFormat": "", "instant": true, "format": "table", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "refId": "B" },
        { "expr": "histogram_quantile(0.95, sum by(method, path, le) (rate(mypresence_http_request_duration_seconds_bucket[5m])))", "legendFormat": "", "instant": true, "format": "table", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "refId": "C" }
      ],
      "transformations": [
        { "id": "merge", "options": {} },
        { "id": "organize", "options": { "renameByName": { "Value #A": "RPS", "Value #B": "Error rate", "Value #C": "P95" } } },
        { "id": "filterFieldsByName", "options": { "include": { "names": ["method", "path", "RPS", "Error rate", "P95"] } } }
      ]
    },


    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 22 }, "id": 20, "title": "🔐 Authentication", "type": "row" },

    {
      "id": 21, "type": "stat",
      "title": "Logins (all time)",
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 23 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "blue" } } },
      "targets": [{ "expr": "sum(mypresence_auth_logins_total)", "legendFormat": "Total logins", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 22, "type": "stat",
      "title": "Login success rate (1h)",
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 23 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "percentunit", "color": { "mode": "thresholds" }, "thresholds": { "mode": "absolute", "steps": [{ "color": "red", "value": null }, { "color": "yellow", "value": 0.8 }, { "color": "green", "value": 0.95 }] } } },
      "targets": [{ "expr": "sum(rate(mypresence_auth_logins_total{result=\"success\"}[1h])) / sum(rate(mypresence_auth_logins_total[1h]))", "legendFormat": "Success rate", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 23, "type": "stat",
      "title": "Failed logins (1h)",
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 23 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "thresholds" }, "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "yellow", "value": 5 }, { "color": "red", "value": 20 }] } } },
      "targets": [{ "expr": "increase(mypresence_auth_logins_total{result=\"failure\"}[1h])", "legendFormat": "Failures", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 24, "type": "stat",
      "title": "Logouts (1h)",
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 23 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "value" },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "increase(mypresence_auth_logouts_total[1h])", "legendFormat": "Logouts", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 25, "type": "timeseries",
      "title": "Login events — by method & result",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 27 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["sum"] } },
      "fieldConfig": { "defaults": { "unit": "short", "custom": { "drawStyle": "bars", "lineWidth": 0, "fillOpacity": 80 } } },
      "targets": [
        { "expr": "increase(mypresence_auth_logins_total[5m])", "legendFormat": "{{method}} – {{result}}", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },

    {
      "id": 26, "type": "barchart",
      "title": "Login breakdown by method",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 27 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "orientation": "auto", "xTickLabelRotation": 0, "legend": { "displayMode": "list", "placement": "bottom" } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [
        { "expr": "sum by(method) (mypresence_auth_logins_total{result=\"success\"})", "legendFormat": "{{method}}", "instant": true, "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },


    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 35 }, "id": 30, "title": "📅 Presence Activity", "type": "row" },

    {
      "id": 31, "type": "stat",
      "title": "Set operations (1h)",
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 36 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "green" } } },
      "targets": [{ "expr": "increase(mypresence_presence_operations_total{action=\"set\"}[1h])", "legendFormat": "Set ops", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 32, "type": "stat",
      "title": "Clear operations (1h)",
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 36 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "orange" } } },
      "targets": [{ "expr": "increase(mypresence_presence_operations_total{action=\"clear\"}[1h])", "legendFormat": "Clear ops", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 33, "type": "stat",
      "title": "Presence days set (1h)",
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 36 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "colorMode": "background" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "teal" } } },
      "targets": [{ "expr": "increase(mypresence_presence_days_total{action=\"set\"}[1h])", "legendFormat": "Days set", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 34, "type": "stat",
      "title": "Total presences recorded",
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 36 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_presences_total", "legendFormat": "Total presences", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 35, "type": "timeseries",
      "title": "Presence operations — set vs clear over time",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 40 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["sum", "lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short", "custom": { "drawStyle": "bars", "lineWidth": 0, "fillOpacity": 70 } } },
      "targets": [
        { "expr": "increase(mypresence_presence_operations_total{action=\"set\"}[5m])", "legendFormat": "Set – {{half}}", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "increase(mypresence_presence_operations_total{action=\"clear\"}[5m])", "legendFormat": "Clear – {{half}}", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },

    {
      "id": 36, "type": "piechart",
      "title": "Presence half-day breakdown (all time)",
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 40 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "pieType": "donut", "legend": { "displayMode": "table", "placement": "right", "calcs": ["value", "percent"] } },
      "targets": [
        { "expr": "sum by(half) (mypresence_presence_operations_total{action=\"set\"})", "legendFormat": "{{half}}", "instant": true, "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },

    {
      "id": 37, "type": "timeseries",
      "title": "Presence days written per minute",
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 40 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "tooltip": { "mode": "multi" } },
      "fieldConfig": { "defaults": { "unit": "short", "custom": { "lineWidth": 2, "fillOpacity": 20 } } },
      "targets": [
        { "expr": "rate(mypresence_presence_days_total{action=\"set\"}[1m]) * 60", "legendFormat": "Days/min set", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "rate(mypresence_presence_days_total{action=\"clear\"}[1m]) * 60", "legendFormat": "Days/min cleared", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },


    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 48 }, "id": 40, "title": "🗄️ Database & App", "type": "row" },

    {
      "id": 41, "type": "stat",
      "title": "Users",
      "gridPos": { "h": 4, "w": 3, "x": 0, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_users_total", "legendFormat": "Users", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 42, "type": "stat",
      "title": "Active sessions",
      "gridPos": { "h": 4, "w": 3, "x": 3, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_sessions_active_total", "legendFormat": "Sessions", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 43, "type": "stat",
      "title": "Teams",
      "gridPos": { "h": 4, "w": 3, "x": 6, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_teams_total", "legendFormat": "Teams", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 44, "type": "stat",
      "title": "Statuses",
      "gridPos": { "h": 4, "w": 3, "x": 9, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_statuses_total", "legendFormat": "Statuses", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 45, "type": "stat",
      "title": "Presences stored",
      "gridPos": { "h": 4, "w": 3, "x": 12, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_presences_total", "legendFormat": "Presences", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 46, "type": "stat",
      "title": "Floor plans",
      "gridPos": { "h": 4, "w": 3, "x": 15, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_floorplans_total", "legendFormat": "Plans", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 47, "type": "stat",
      "title": "Seats",
      "gridPos": { "h": 4, "w": 3, "x": 18, "y": 49 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "expr": "mypresence_db_seats_total", "legendFormat": "Seats", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 48, "type": "timeseries",
      "title": "DB growth — users & presences over time",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 53 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["lastNotNull"] } },
      "fieldConfig": { "defaults": { "unit": "short", "custom": { "lineWidth": 2, "fillOpacity": 10 } } },
      "targets": [
        { "expr": "mypresence_db_users_total", "legendFormat": "Users", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "mypresence_db_sessions_active_total", "legendFormat": "Active sessions", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "mypresence_db_presences_total / 100", "legendFormat": "Presences ÷ 100", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },


    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 61 }, "id": 50, "title": "⚙️ Go Runtime", "type": "row" },

    {
      "id": 51, "type": "timeseries",
      "title": "Goroutines",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 62 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "legend": { "displayMode": "list" } },
      "fieldConfig": { "defaults": { "unit": "short", "custom": { "lineWidth": 2, "fillOpacity": 15 } } },
      "targets": [{ "expr": "go_goroutines", "legendFormat": "Goroutines", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 52, "type": "timeseries",
      "title": "Heap memory",
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 62 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "legend": { "displayMode": "table", "calcs": ["lastNotNull", "max"] } },
      "fieldConfig": { "defaults": { "unit": "bytes", "custom": { "lineWidth": 2, "fillOpacity": 15 } } },
      "targets": [
        { "expr": "go_memstats_heap_alloc_bytes", "legendFormat": "Heap alloc", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "go_memstats_heap_sys_bytes", "legendFormat": "Heap sys", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },

    {
      "id": 53, "type": "timeseries",
      "title": "GC pause duration",
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 62 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": { "legend": { "displayMode": "list" } },
      "fieldConfig": { "defaults": { "unit": "ns", "custom": { "lineWidth": 2 } } },
      "targets": [
        { "expr": "rate(go_gc_duration_seconds_sum[5m]) / rate(go_gc_duration_seconds_count[5m])", "legendFormat": "Avg GC pause", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    },


    { "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 68 }, "id": 60, "title": "🩺 App Health", "type": "row" },

    {
      "id": 61, "type": "stat",
      "title": "App status",
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 69 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"] },
        "orientation": "auto",
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "auto",
        "text": {}
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "mappings": [
            { "type": "value", "options": { "0": { "text": "DOWN",  "color": "red"   }, "1": { "text": "UP", "color": "green" } } }
          ],
          "color": { "mode": "thresholds" },
          "thresholds": { "mode": "absolute", "steps": [{ "color": "red", "value": null }, { "color": "green", "value": 1 }] }
        }
      },
      "targets": [{ "expr": "mypresence_up", "legendFormat": "App", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 62, "type": "stat",
      "title": "Database status",
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 69 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"] },
        "orientation": "auto",
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "mappings": [
            { "type": "value", "options": { "0": { "text": "DOWN", "color": "red" }, "1": { "text": "UP", "color": "green" } } }
          ],
          "color": { "mode": "thresholds" },
          "thresholds": { "mode": "absolute", "steps": [{ "color": "red", "value": null }, { "color": "green", "value": 1 }] }
        }
      },
      "targets": [{ "expr": "mypresence_db_up", "legendFormat": "DB", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 63, "type": "stat",
      "title": "Uptime",
      "gridPos": { "h": 4, "w": 12, "x": 12, "y": 69 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"] },
        "orientation": "auto",
        "colorMode": "background",
        "graphMode": "area",
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "color": { "mode": "fixed", "fixedColor": "blue" }
        }
      },
      "targets": [{ "expr": "mypresence_uptime_seconds", "legendFormat": "Uptime", "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }]
    },

    {
      "id": 64, "type": "timeseries",
      "title": "App & DB health over time",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 73 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["lastNotNull", "min"] }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "min": 0, "max": 1,
          "custom": { "lineWidth": 2, "fillOpacity": 20 },
          "mappings": [
            { "type": "value", "options": { "0": { "text": "0 (DOWN)" }, "1": { "text": "1 (UP)" } } }
          ],
          "color": { "mode": "palette-classic" }
        }
      },
      "targets": [
        { "expr": "mypresence_up",    "legendFormat": "App up",    "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } },
        { "expr": "mypresence_db_up", "legendFormat": "DB up",     "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" } }
      ]
    }

  ],
  "refresh": "30s",
  "schemaVersion": 37,
  "style": "dark",
  "tags": ["mypresence", "go", "presence"],
  "templating": {
    "list": [
      {
        "current": {},
        "hide": 0,
        "includeAll": false,
        "name": "DS_PROMETHEUS",
        "options": [],
        "query": "prometheus",
        "refresh": 1,
        "type": "datasource",
        "label": "Datasource"
      }
    ]
  },
  "time": { "from": "now-3h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "myPresence – Operations",
  "uid": "mypresence-ops",
  "version": 1
}
